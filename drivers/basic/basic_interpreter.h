#ifndef BASIC_INTERPRETER_H
#define BASIC_INTERPRETER_H

// This is the only function the outside world needs to know!
// Give it code, and it will cause chaos!
void interpret_program(const char* code);

#endif